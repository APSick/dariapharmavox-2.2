<% layout('../partials/layout') %>

    <section class="space-y-6">
        <header class="space-y-2">
            <h1 class="text-2xl sm:text-3xl font-semibold text-slate-50">
                Фарма-жизнь
            </h1>
            <p class="text-sm sm:text-base text-slate-200/90 max-w-3xl">
                Небольшие заметки о студенческой жизни на ФФМ МГУ: конференции и школы Сколково, волонтёрство, лабораторные, экскурсии в исследовательские центры, курсы по базовой реанимации и встречи с фармкомпаниями. Здесь я фиксирую моменты, которые формируют мой
                путь в фармацию и помогают взглянуть на профессию шире, чем только учебный план.
            </p>
        </header>

        <% if (posts.length === 0) { %>
            <p class="text-sm text-slate-300/85">
                Записей пока нет. Скоро здесь появятся истории о «реальной» фарма-жизни.
            </p>
            <% } else { %>
                <div class="space-y-3">
                    <% posts.forEach(function(post){ %>
                        <article class="rounded-2xl border border-white/10 bg-slate-900/70 px-4 py-3 hover:border-sky-400/80 hover:-translate-y-0.5 hover:shadow-md hover:shadow-sky-700/40 transition">
                            <a href="/pharma-life/<%= post.slug %>" class="block">
                                <div class="flex items-start justify-between gap-3">
                                    <div>
                                        <h2 class="text-sm sm:text-base font-semibold text-slate-50">
                                            <%= post.title %>
                                        </h2>
                                        <% if (post.summary) { %>
                                            <p class="mt-1 text-xs text-slate-300/90 line-clamp-2">
                                                <%= post.summary %>
                                            </p>
                                            <% } %>
                                    </div>
                                    <div class="text-right text-[11px] text-slate-400/85 shrink-0">
                                        <p>
                                            <%= new Date(post.created_at).toLocaleDateString('ru-RU') %>
                                        </p>
                                        <p class="mt-0.5 flex items-center gap-2 justify-end">
                                            <span>
      👁 <span class="font-semibold text-sky-200"><%= post.views %></span>
                                            </span>
                                            <span>
      ❤️ <span class="font-semibold text-rose-200"><%= post.likes || 0 %></span>
                                            </span>
                                        </p>
                                    </div>

                                </div>
                            </a>
                        </article>
                        <% }) %>
                </div>
                <% } %>
    </section>