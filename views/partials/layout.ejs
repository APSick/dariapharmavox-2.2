<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8" />
    <title>
        <%= title || 'Daria Pharma Vox' %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- Tailwind через CDN -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Шрифт -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />

    <link rel="stylesheet" href="/css/styles.css" />
</head>

<body class="min-h-screen text-slate-100 bg-slate-950 font-['Inter'] relative overflow-x-hidden">

    <!-- Анимированный фон -->
    <div class="fixed inset-0 -z-20 bg-gradient-to-br from-indigo-900 via-slate-900 to-sky-900 animate-gradient"></div>

    <!-- “Светящиеся круги” -->
    <div class="fixed -top-32 -left-32 w-80 h-80 bg-indigo-500/30 rounded-full blur-3xl -z-10"></div>
    <div class="fixed top-1/2 -right-32 w-80 h-80 bg-sky-400/25 rounded-full blur-3xl -z-10"></div>
    <div class="fixed -bottom-32 left-1/3 w-72 h-72 bg-emerald-400/20 rounded-full blur-3xl -z-10"></div>

    <div class="relative z-10">
        <% include ./navbar %>

            <main class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8 sm:py-12">
                <% include ./messages %>
                    <%- body %>
            </main>

            <footer class="border-t border-white/10 mt-8">
                <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex flex-col sm:flex-row justify-between items-center gap-2 text-sm text-slate-300/80">
                    <p>©
                        <%= new Date().getFullYear() %> Daria Pharma Vox</p>
                    <p>Сайт-визитка: фармация, наука, студенческая жизнь МГУ (ФФМ)</p>
                </div>
            </footer>
    </div>
</body>

</html>